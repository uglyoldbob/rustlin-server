pub mod packet;

pub fn do_stuff() {
    log::info!("I am doing stuff");
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
